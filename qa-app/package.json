{
  "name": "qa-app",
  "private": true,
  "version": "1.0.0",
  "description": "Quality Assurance Management Application (dashboard + 5 modules).",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install && npm --prefix backend run db:push && npm --prefix backend run seed",
    "seed": "npm --prefix backend run seed",
    "test": "npm --prefix backend test",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
